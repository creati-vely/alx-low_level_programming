#include "main.h"
#include <stdlib.h>

/**
 * _puts - prints the string followed by a new line.
 * @*str: puts out the string stored in str.
 * Return: Always 0
 */

void _puts(char *str)
{
	int i;


	while (*str != '\0')
	{
		i++;
	}
	return (0);
}
