#include "main.h"
#include <stdlib.h>
#include <string.h>

/**
 * _strlen - it returns the length of a string
 *
 * @s: store the length of string
 * Return: len
 */

int _strlen(char *s)
{
	return (strlen(s));
}

